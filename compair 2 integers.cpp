#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"enter first value, a="; cin>>a;
    cout<<"enter second value, b="; cin>>b;
    if(a>b)
    {
        cout<<"a"<<a<<" is greater.";
    }
    else
    {
        cout<<"b"<<b<<" is greater.",b;
    }
    
}